services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    volumes:
      - app_storage:/app/storage
    ports:
      - "8000:8000"
    restart: unless-stopped

volumes:
  app_storage:
